{# /src/Finance/OperationBundle/Resources/views/AbstractOperation/see.html.twig #}

{% extends "::base.html.twig" %}

{% block title %}
{{abstractoperation}} - {%trans%}Operation{%endtrans%} - {{ parent() }}
{% endblock %}

{% block body -%}
<h1>{%trans%}AbstractOperation{%endtrans%} {{abstractoperation}}</h1>

{# -------------------------------------------------------------------------- #}
{# ------------------------------- Sub nav ---------------------------------- #}
{% include '::confirmDelete.html.twig' %}
{#<div class="nav subnav">    
    {% set seeMenu = knp_menu_get( 'FinanceOperationBundle:BuilderAbstractOperation:seeMenu' , [], {'abstractoperation': abstractoperation}) %} 
    {{ mopa_bootstrap_menu(seeMenu, {'automenu': 'pills', 'stacked':false}) }}
</div>#}
{% embed '::seeMenu.html.twig' with {
    routePrefix:    'finance_operation_abstractoperation_',
    entityId:       'abstractoperation_id',
    entity:         abstractoperation
} %}
{% endembed %}


{# -------------------------------------------------------------------------- #}
{# ------------------------------ AbstractOperation info -------------------------------- #}
<table class="table table-hover table-condensed">
    <tr>
        <th>{%trans%}Date{%endtrans%}</th>
        <td>{{ abstractoperation.date|date("Y-m-d") }}</td>
    </tr>
    <tr>
        <th>{%trans%}Amount{%endtrans%}</th>
        <td>{{abstractoperation.amount}}</td>
    </tr>
    <tr>
        <th>{%trans%}IsMarked{%endtrans%}</th>
        <td>{{abstractoperation.isMarked}}</td>
    </tr>
</table>

{% endblock %}
